---
'@graphql-hive/router-runtime': minor
---

### Support remote cache for `router-runtime`

The (remote) `cache` provided for the gateway is now used also to store query plans object created by the Router (Rust) runtime.
